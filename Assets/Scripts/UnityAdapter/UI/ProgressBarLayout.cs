using UnityEngine;
using UnityEngine.UI;
using TMPro;

namespace BlockPuzzle.UnityAdapter.UI
{
    /// <summary>
    /// Deprecated compatibility stub.
    /// Production target/progress UI is owned directly by TargetGoalSystem.
    /// Keep this type only to avoid risky scene/prefab breakage until a later cleanup removes the stub after a fresh reference audit.
    /// </summary>
    [AddComponentMenu("")]
    [System.Obsolete("Inline in TargetGoalSystem for better project structure")]
    public class ProgressBarLayout : MonoBehaviour
    {
        // Intentionally empty.
    }
}
